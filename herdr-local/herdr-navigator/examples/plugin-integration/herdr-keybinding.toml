# Example Herdr keybinding for Herdr Navigator.
# Add this to ~/.config/herdr/config.toml.

[[keys.command]]
key = "prefix+t"
type = "plugin_action"
command = "herdr-navigator.open"
description = "picker center"
